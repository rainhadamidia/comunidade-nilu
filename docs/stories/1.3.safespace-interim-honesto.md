# Story 1.3: Interim Honesto do SafeSpace — Desabilitar Publicação Enquanto Não Persiste de Verdade

## Status

Ready

## Executor Assignment

executor: "@dev"
quality_gate: "@ux-design-expert"
quality_gate_tools: [tone_review, ux_consistency_check]

> Nota de escopo: esta é a terceira e última metade da "Story 1" do Epic 01 (ver `docs/stories/epics/epic-01-fase1-fundacao-seguranca-lgpd-safespace/epic.md`). As outras duas são a **Story 1.1** (CI Gate, `@devops`) e a **Story 1.2** (modelo LGPD, `@data-engineer`). As 3 são independentes entre si — todas precisam estar prontas antes da Story 2 começar, mas podem ser implementadas em paralelo.
>
> Quality gate é `@ux-design-expert` (não `@architect`, diferente das outras duas stories do Lote 0) porque o risco principal aqui é de **tom/mensagem** — a recomendação da Uma no assessment é sobre como comunicar a limitação sem soar técnico ou frio, dado o contexto de saúde emocional.

## Story

**As a** usuário do ILUMINNARE que quer desabafar no SafeSpace,
**I want** que o app me diga a verdade sobre o que acontece com o que eu escrevo — em vez de fingir que meu desabafo foi salvo quando na verdade ele some no próximo refresh,
**so that** eu não seja enganado sobre a privacidade/persistência do meu conteúdo sensível enquanto o backend real (Story 3) não sobe. Hoje o botão "Compartilhar anonimamente" mostra um toast de sucesso e o texto "Sua identidade está protegida" — ambos enganosos, porque nada é persistido (`FE-02`, `docs/prd/technical-debt-assessment.md` linha 93).

## Acceptance Criteria

1. O botão de publicar (atualmente "Compartilhar anonimamente" em `src/pages/SafeSpace.tsx`) fica **desabilitado** (`disabled` nativo do HTML, que já implica `aria-disabled` para leitores de tela), com um indicador visual `Badge` (componente já existente em `src/components/ui/badge.tsx`) mostrando "Em breve" próximo ao título ou ao formulário. O texto do `Badge` deve ser lido por leitor de tela como parte do fluxo normal (não apenas visual) — não introduzir débito de acessibilidade novo em cima do `FE-04` já catalogado.
2. O texto "Sua identidade está protegida" (linha 100 de `SafeSpace.tsx`) é substituído por uma mensagem honesta e acolhedora explicando que a publicação real ainda não está disponível — sem jargão técnico ("backend", "banco de dados", "persistência"). Tom segue a recomendação da Uma (@ux-design-expert) citada no assessment: "interim honesto obrigatório... mentir persistência é pior que não ter" (`docs/prd/technical-debt-assessment.md` linha 152).
3. `handlePost()` não executa mais o fluxo de "sucesso" (nada de `setPosts([post, ...posts])` fingindo publicar, nada do toast "Desabafo compartilhado"). O textarea pode continuar visível/editável (rascunho local), mas nenhuma ação de envio finaliza como se tivesse persistido.
4. A feed de posts mockados existentes (visualização, curtidas, comentários) **continua funcionando normalmente** — este interim é só sobre a ação de *criar novo post*, não sobre esconder a tela inteira.
5. A mudança é controlada por uma constante simples e explícita no topo do arquivo (ex.: `const SAFESPACE_PERSISTENCE_ENABLED = false`), para que a Story 3 (quando a persistência real subir) troque só essa flag/implementação em vez de reescrever a tela — sem introduzir um sistema de feature flags novo/genérico (YAGNI: é 1 tela, não precisa de infraestrutura de flags).
6. Nenhuma mudança nesta story depende da Story 1.1 ou 1.2 — é puramente frontend, isolada em `SafeSpace.tsx`.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Sem `core-config.yaml`/`coderabbit_integration.enabled` configurado neste projeto. Quality gate é a revisão manual do `@ux-design-expert` (tom/mensagem) descrita acima.

## Tasks / Subtasks

- [ ] Task 1 — Constante de controle (AC: 5)
  - [ ] Adicionar `const SAFESPACE_PERSISTENCE_ENABLED = false;` no topo de `src/pages/SafeSpace.tsx`
- [ ] Task 2 — Desabilitar a ação de publicar (AC: 1, 3)
  - [ ] Envolver o botão de envio (linhas 108-114) com a condição `disabled={!SAFESPACE_PERSISTENCE_ENABLED}`
  - [ ] Remover/guardar o corpo de `handlePost()` (linhas 15-39) atrás de `if (!SAFESPACE_PERSISTENCE_ENABLED) return;` — sem apagar a lógica (será reativada na Story 3), só bloquear a execução
  - [ ] Adicionar `Badge` (importar de `@/components/ui/badge`, já existe no projeto) com texto "Em breve" próximo ao título "Espaço Seguro" (linha 87-90)
- [ ] Task 3 — Mensagem honesta (AC: 2)
  - [ ] Substituir o texto da linha 100 ("Sua identidade está protegida") por uma mensagem que comunique, em tom acolhedor: que a publicação real chega em breve, sem prometer persistência que não existe hoje
  - [ ] Validar o texto final com `@ux-design-expert` antes de fechar a story (quality gate)
- [ ] Task 4 — Confirmar que a feed de leitura não é afetada (AC: 4)
  - [ ] Testar manualmente: curtir e comentar em posts mockados existentes continuam funcionando (essas ações não fingem persistir nada novo — já eram só de exibição)

## Dev Notes

### Contexto do Epic

Terceira e última parte do Lote 0 (Story 1 do Epic 01). Sem dependência técnica das Stories 1.1/1.2 — pode ser feita a qualquer momento, mas logicamente faz parte do mesmo "pacote" de pré-requisitos antes da Story 2. [Source: docs/stories/epics/epic-01-fase1-fundacao-seguranca-lgpd-safespace/epic.md#story-1]

### Estado atual do código (verificado, arquivo lido integralmente)

`src/pages/SafeSpace.tsx` (206 linhas) hoje:
- Linha 10: `const [posts, setPosts] = useState<SafeSpacePost[]>(initialPosts);` — estado local, `initialPosts` vem de `src/lib/mockData.ts`
- Linhas 15-39: `handlePost()` — valida tamanho mínimo (10 chars), cria um objeto `SafeSpacePost` com `Date.now()` como id, dá `setPosts([post, ...posts])` e mostra toast de sucesso "Desabafo compartilhado" / "Sua mensagem foi publicada anonimamente" — **isso é o comportamento enganoso a remover/bloquear**
- Linha 100: texto "Sua identidade está protegida" — mensagem hoje implica persistência segura que não existe
- Linhas 108-114: botão "Compartilhar anonimamente", `onClick={handlePost}`
- Linhas 117-202: feed de posts (leitura, `handleLike`, `handleComment`) — **não mexer**, continua mockado normalmente, isso não é enganoso da mesma forma (o assessment foca no ato de *criar* um desabafo, que é o dado sensível)
- Imports já disponíveis na tela: `Button`, `Textarea`, `toast` (de `@/hooks/use-toast`), ícones `lucide-react` (`Shield`, `Eye`, `EyeOff`, etc.) [Source: src/pages/SafeSpace.tsx linhas 1-206]

### Componente `Badge` já existe — reusar, não criar

`src/components/ui/badge.tsx` já existe no projeto (shadcn-ui). Usar o padrão já estabelecido em vez de criar um componente novo de "tag"/"chip". [Fonte: verificação direta do repositório]

### Por que não um sistema de feature flags genérico

O assessment não pede infraestrutura de feature flags — é 1 tela, 1 estado binário, temporário (só até a Story 3 subir). Uma constante local é suficiente e mais simples; não inventar abstração além do necessário (princípio YAGNI do projeto).

### O que esta story NÃO faz

- Não implementa persistência real (Story 3, depende também da Story 1.2 estar aprovada).
- Não mexe no restante da UI/tom visual do SafeSpace (isso é `FE-01`/`FE-07`, Epic 02 futura).
- Não decide o texto final sozinho — precisa da revisão de tom do `@ux-design-expert` (quality gate) antes de fechar.

### Testing

- Teste manual (não há suíte de testes de UI automatizada ainda além do que a Story 1.1 está montando): confirmar que o botão está desabilitado, que nenhum toast de "sucesso" aparece ao tentar publicar, e que curtir/comentar em posts existentes continua funcionando.
- Se a Story 1.1 (CI/vitest) já estiver mesclada quando esta for implementada, considerar adicionar 1 teste de componente simples (`@testing-library/react`) confirmando `disabled` no botão — não obrigatório para fechar esta story, mas recomendado.

## Change Log

| Data | Versão | Descrição | Autor |
|---|---|---|---|
| 2026-07-04 | 0.1 | Story criada a partir do Epic 01, Story 1 (metade interim honesto do SafeSpace) — `@sm *draft` | River (@sm) |
| 2026-07-04 | 0.2 | Validated GO (7,5/10) — Status: Draft → Ready. Ajuste aplicado no AC 1: exigir que o estado desabilitado seja acessível a leitor de tela (não só visual), para não somar débito novo sobre o `FE-04` já catalogado. | Pax (@po) |

## Dev Agent Record

### Agent Model Used

_A ser preenchido pelo @dev durante a implementação._

### Debug Log References

_A ser preenchido pelo @dev durante a implementação._

### Completion Notes List

_A ser preenchido pelo @dev durante a implementação._

### File List

_A ser preenchido pelo @dev durante a implementação._

## QA Results

_A ser preenchido pelo @qa após a implementação._
